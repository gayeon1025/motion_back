package com.cnu.motion.common.type;

public enum Grade {
    FIRST_GRADE(1),
    SECOND_GRADE(2),
    THIRD_GRADE(3),
    FOURTH_GRADE(4);

    public final int grade;

    Grade(int grade) { this.grade = grade; }
}
